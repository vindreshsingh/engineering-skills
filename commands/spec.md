---
description: Write a specification before coding using the spec-first skill.
---

Follow the `spec-first` skill (`skills/spec-first/SKILL.md`) for this task. Produce a specification
before writing any implementation code. If requirements are unclear or only a vague idea exists,
clarify them first per the skill's process.

$ARGUMENTS
