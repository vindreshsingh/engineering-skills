---
description: Implement incrementally with tests using incremental-delivery + test-first.
---

Follow the `incremental-delivery` skill (`skills/incremental-delivery/SKILL.md`) together with the
`test-first` skill (`skills/test-first/SKILL.md`). Implement in small, verifiable increments and
prove each one with tests before moving on.

$ARGUMENTS
