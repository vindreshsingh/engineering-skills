---
description: Find and fix the real bottleneck using the perf-budget skill.
---

Follow the `perf-budget` skill (`skills/perf-budget/SKILL.md`). Define a target, measure to locate the
dominant cost, fix that first, and confirm with before/after numbers.

$ARGUMENTS
