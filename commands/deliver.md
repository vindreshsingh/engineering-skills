---
description: Drive a feature end-to-end (define → ship) with the orchestrated-delivery skill.
---

Follow the `orchestrated-delivery` skill (`skills/orchestrated-delivery/SKILL.md`). Run this work
through the lifecycle — define, plan, build, verify, review, ship — loading the governing skill and
persona at each phase, stopping at the Define and Plan gates for sign-off, and dispatching independent
tasks to parallel subagents (`parallel-subagents`). Keep one written plan as the source of truth.

$ARGUMENTS
