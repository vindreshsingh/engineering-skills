---
description: Debug a failure methodically using the fault-recovery skill.
---

Follow the `fault-recovery` skill (`skills/fault-recovery/SKILL.md`). Reproduce the failure, read the
error, narrow by bisecting, test one hypothesis at a time, and fix the root cause with a regression test.

$ARGUMENTS
