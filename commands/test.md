---
description: Drive changes with tests using the test-first skill.
---

Follow the `test-first` skill (`skills/test-first/SKILL.md`) for this task. Write failing tests that
capture the intended behavior, then implement until they pass.

$ARGUMENTS
