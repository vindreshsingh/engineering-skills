---
description: Apply security fundamentals to the change using the hardening skill.
---

Follow the `hardening` skill (`skills/hardening/SKILL.md`). Treat input as hostile: validate at the
boundary, check authorization, protect secrets, and prefer safe defaults that fail closed.

$ARGUMENTS
